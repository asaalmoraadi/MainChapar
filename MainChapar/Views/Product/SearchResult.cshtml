@model List<MainChapar.Models.Product>

<h2>نتایج جستجو</h2>

@if (Model.Count == 0)
{
    <p>هیچ محصولی یافت نشد.</p>
}
else
{
    <ul>
        @foreach (var product in Model)
        {
            <li>@product.Title - قیمت: @product.Price تومان</li>
        }
    </ul>
}
